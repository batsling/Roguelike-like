# Pacer art drop

The Pacer is the **shared headless-body walk cycle** — this same body is reused
by the Gusher. Drop the raw walk sheet here; tell me the grid (cols × rows,
which rows = down/up/side, which frames = death). I'll slice → rename to:

**body (directional; `side` faces RIGHT, left mirrored)**
- `pacer_walk_down_*.png`
- `pacer_walk_up_*.png`
- `pacer_walk_side_*.png`
- `pacer_death_*.png`

Suggested raw filename to drop:
- `pacer_body_sheet.png`
